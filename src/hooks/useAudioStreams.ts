
/**
 * Disabled audio streams hook - all audio handling now in SipCallManager
 */
export const useAudioStreams = (isCallActive: boolean) => {
  // No-op - SipCallManager handles all audio streams directly
};
